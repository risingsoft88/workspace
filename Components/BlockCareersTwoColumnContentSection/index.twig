<section class="block-careers-two-column-content-section {% if border_bottom %} border-bottom {% endif %}">
    <div class="container">
        <div class="block-careers-two-column-content-row">
            <div class="left-col">
                {{ content_left|e('wp_kses_post') }}
            </div>
            <div class="right-col">
                {{ content_right|e('wp_kses_post') }}
            </div>
        </div>
    </div>
</section>