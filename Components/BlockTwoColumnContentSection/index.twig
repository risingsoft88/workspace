<section {% if section_id %} id="{{ section_id }}" {% endif %} class="block-two-column-content-section">
    <div class="container">
        <div class="block-two-column-content-row">
            <div class="left-col">
                {{ content_left|e('wp_kses_post') }}
            </div>
            <div class="right-col">
                {{ content_right|e('wp_kses_post') }}
            </div>
        </div>
    </div>
</section>