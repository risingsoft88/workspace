{% extends 'templates/_document.twig' %}

{% block content %}
  {% for component in post.meta('postBenefitComponents') %}
    {{ renderComponent(component) }}
  {% endfor %}

  {{ renderComponent('PostPrevNextLinkSection', { prev: post.prev, next: post.next }) }}
    
  {{ renderComponent('RequestDemoSection', { background_image: benefit_footer_form_background_image, content: benefit_footer_form_content, gravity_grom_shortcode: benefit_footer_gravity_form_shortcode, align_center: true, is_hero: false }) }}
{% endblock %}